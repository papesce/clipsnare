services:
  clipsnare:
    image: papesce/clipsnare:latest
    container_name: clipsnare
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 5173
      HOST: 0.0.0.0
    ports:
      - "3005:5173"
