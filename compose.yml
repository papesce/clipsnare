services:
  clipsnare:
    build: .
    container_name: clipsnare
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 5173
      BRAND_LABEL: "My Provider"
    ports:
      - "5173:5173"
